{
  "name": "server3_azure-graph-apis",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "echo npm ERROR: * You must be in an app folder to start app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-server": "^13.0.1",
    "@azure/msal-node": "^1.17.3",
    "@microsoft/teamsfx": "^2.2.1",
    "axios": "^1.4.0",
    "dotenv": "^16.3.1",
    "yargs": "^17.7.2"
  }
}
